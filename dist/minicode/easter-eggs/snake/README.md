# SnakeGame

这是 MiniCode 自己在 `manual-test-workspace` 多轮测试中堆出来的 Swing 贪吃蛇小游戏。

它作为 MiniCode4j 的小彩蛋保留在独立目录中：

```text
easter-eggs\snake\
  SnakeGame.java
  snake.jar
```

运行：

```powershell
minicode --snake
```

也可以直接运行独立 jar：

```powershell
java -jar easter-eggs\snake\snake.jar
```
